{% extends 'base.html.twig' %}

{% block title %}Attestation{% endblock %}

{% block body %}
    <h1>Attestation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ attestation.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ attestation.Date ? attestation.Date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TotalPoints</th>
                <td>{{ attestation.TotalPoints }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('attestation_index') }}">back to list</a>

    <a href="{{ path('attestation_edit', {'id': attestation.id}) }}">edit</a>

    {{ include('attestation/_delete_form.html.twig') }}
{% endblock %}
