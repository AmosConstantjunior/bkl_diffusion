{% extends 'base.html.twig' %}

{% block title %}Edit Evaluer{% endblock %}

{% block body %}
    <h1>Edit Evaluer</h1>

    {{ include('evaluer/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('evaluer_index') }}">back to list</a>

    {{ include('evaluer/_delete_form.html.twig') }}
{% endblock %}
