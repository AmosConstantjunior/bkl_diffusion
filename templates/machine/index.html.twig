{% extends 'base.html.twig' %}

{% block title %}Machine index{% endblock %}

{% block body %}
    <h1>Machine index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NumSerie</th>
                <th>VersionEprom</th>
                <th>ProchaineIntervention</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for machine in machines %}
            <tr>
                <td>{{ machine.id }}</td>
                <td>{{ machine.NumSerie }}</td>
                <td>{{ machine.VersionEprom }}</td>
                <td>{{ machine.ProchaineIntervention ? machine.ProchaineIntervention|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('machine_show', {'id': machine.id}) }}">show</a>
                    <a href="{{ path('machine_edit', {'id': machine.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('machine_new') }}">Create new</a>
{% endblock %}
