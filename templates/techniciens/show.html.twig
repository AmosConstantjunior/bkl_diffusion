{% extends 'base.html.twig' %}

{% block title %}Techniciens{% endblock %}

{% block body %}
    <h1>Techniciens</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ technicien.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ technicien.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ technicien.roles ? technicien.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ technicien.password }}</td>
            </tr>
            <tr>
                <th>NomTechnicien</th>
                <td>{{ technicien.NomTechnicien }}</td>
            </tr>
            <tr>
                <th>PrenomTechnicien</th>
                <td>{{ technicien.PrenomTechnicien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('techniciens_index') }}">back to list</a>

    <a href="{{ path('techniciens_edit', {'id': technicien.id}) }}">edit</a>

    {{ include('techniciens/_delete_form.html.twig') }}
{% endblock %}
