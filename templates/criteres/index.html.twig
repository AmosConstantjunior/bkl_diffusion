{% extends 'base.html.twig' %}

{% block title %}Criteres index{% endblock %}

{% block body %}
    <h1>Criteres index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Intituler</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for critere in criteres %}
            <tr>
                <td>{{ critere.id }}</td>
                <td>{{ critere.Intituler }}</td>
                <td>
                    <a href="{{ path('criteres_show', {'id': critere.id}) }}">show</a>
                    <a href="{{ path('criteres_edit', {'id': critere.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('criteres_new') }}">Create new</a>
{% endblock %}
