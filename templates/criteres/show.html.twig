{% extends 'base.html.twig' %}

{% block title %}Criteres{% endblock %}

{% block body %}
    <h1>Criteres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ critere.id }}</td>
            </tr>
            <tr>
                <th>Intituler</th>
                <td>{{ critere.Intituler }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('criteres_index') }}">back to list</a>

    <a href="{{ path('criteres_edit', {'id': critere.id}) }}">edit</a>

    {{ include('criteres/_delete_form.html.twig') }}
{% endblock %}
